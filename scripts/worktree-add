#!/usr/bin/env bash
set -euo pipefail

name="${1:?Usage: worktree-add <name>}"
main_repo="$(pwd)"
dest="../$name"

git worktree add -b "$name" "$dest" main

for d in third_party wz; do
  rm -rf "$dest/$d"
  ln -s "$main_repo/$d" "$dest/$d"
done

echo "Worktree '$name' created at $dest"
