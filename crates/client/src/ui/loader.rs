// UI sprite loader.
//
// Intentionally empty for now: it depended on `WzUiSpriteAsset`, which is not
// implemented yet. The `WzAsset` derive + `WzAssetLoader` path lives in
// `crate::wz`; re-add the sprite-handle types here once that asset exists.
