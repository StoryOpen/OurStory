// Logo screen.
//
// Intentionally empty for now: it depended on `WzImageFramesAsset`, which is
// not implemented yet. Re-add the logo load/animation systems here once that
// asset exists, and wire them back into `UiPlugin`.
